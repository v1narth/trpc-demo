import { NavLink } from 'react-router-dom';

const Index = () => {
  return (
    <div>
      <div>Redis + tRPC = ...</div>
      <NavLink to='/tasks'>Tasks</NavLink>
    </div>
  );
};

export default Index;
